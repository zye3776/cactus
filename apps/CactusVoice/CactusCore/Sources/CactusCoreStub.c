/* CactusCore stub TU.
 *
 * Static libraries require at least one compiled object. This translation
 * unit is intentionally empty; real C wrappers over the cactus C++ API
 * arrive in Story 1.2 (`cactus_c.cpp`).
 */
#include "CactusCore.h"

/* Marker symbol so the resulting `.a` is non-empty on every toolchain. */
const int cactus_core_skeleton_marker = 0;
